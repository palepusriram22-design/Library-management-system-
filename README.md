# Library-management-system-
A simple Library Management System in C that allows users to add, view, search, and delete book records. It uses file handling to store data permanently and provides an easy-to-use menu-driven interface for efficient library operations.
